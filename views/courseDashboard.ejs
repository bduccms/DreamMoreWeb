<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Course Dashboard: <%= course.title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <style>
    :root {
      --primary: #ff7800;
      --secondary: #ffffff;
      --accent: #010318;
    }
    body {
      background-color: #f8f9fa;
      font-family: 'Inter', sans-serif;
    }
    .course-container {
      background: var(--secondary);
      border-radius: 12px;
      box-shadow: 0 4px 12px rgba(1, 3, 24, 0.08);
      padding: 2rem;
    }
    .section-title {
      color: var(--accent);
      position: relative;
      padding-bottom: 10px;
      font-weight: 700;
    }
    .section-title::after {
      content: '';
      position: absolute;
      left: 0;
      bottom: 0;
      width: 50px;
      height: 4px;
      background-color: var(--primary);
      border-radius: 2px;
    }
    .material-card {
      border-radius: 8px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
    }
    .material-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(1, 3, 24, 0.1);
    }
    .btn-primary {
      background-color: var(--primary);
      border-color: var(--primary);
      color: var(--secondary);
      border-radius: 8px;
      font-weight: 600;
      transition: background-color 0.3s ease, transform 0.2s ease;
    }
    .btn-primary:hover {
      background-color: #e06b00;
      border-color: #e06b00;
      transform: translateY(-2px);
    }
    .error-message {
      color: #dc3545;
      text-align: center;
      margin-bottom: 1rem;
    }
    @media (max-width: 576px) {
      .course-container {
        padding: 1rem;
      }
      .section-title {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <header class="py-6" style="background-color: var(--accent); color: var(--secondary);">
    <h1 class="text-3xl font-bold text-center">Course Dashboard: <%= course.title %></h1>
  </header>
  <main class="container mx-auto py-8">
    <% if (error) { %>
      <div class="error-message text-danger text-center"><%= error %></div>
    <% } %>
    <div class="course-container">
      <div class="d-flex justify-content-between align-items-center mb-5 flex-wrap">
        <div class="mb-3 mb-md-0">
          <h2 class="section-title"><%= course.title %></h2>
          <p class="text-muted"><%= course.description || 'Welcome to your course dashboard!' %></p>
        </div>
        <a href="/courses" class="btn btn-primary">Back to Courses</a>
      </div>
      <div class="row">
        <div class="col-md-6 mb-4">
          <div class="card material-card h-100">
            <img src="/Uploads/<%= course.photo || 'default.jpg' %>" alt="<%= course.title %>" class="card-img-top" style="height: 200px; object-fit: cover; border-radius: 8px 8px 0 0;">
            <div class="card-body">
              <h5 class="card-title" style="color: var(--accent); font-weight: 600;">Course Overview</h5>
              <p class="card-text text-muted">Access all materials for <%= course.title %> below. Download resources to continue your learning journey.</p>
            </div>
          </div>
        </div>
        <div class="col-md-6 mb-4">
          <div class="card material-card h-100">
            <div class="card-body">
              <h5 class="card-title" style="color: var(--accent); font-weight: 600;">Course Materials</h5>
              <ul class="list-group list-group-flush">
                <% if (course.materials && course.materials.length > 0) { %>
                  <% course.materials.forEach(material => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                      <span><%= material.name %></span>
                      <a href="/uploads/materials/<%= material.file %>" class="btn btn-sm btn-success" download>
                        <i class="bi bi-download me-1"></i> Download
                      </a>
                    </li>
                  <% }) %>
                <% } else { %>
                  <li class="list-group-item text-muted">No materials available yet.</li>
                <% } %>
              </ul>
            </div>
          </div>
        </div>
      </div>
    </div>
  </main>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>