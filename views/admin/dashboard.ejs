<% include('../partials/header', { title: 'Dashboard' }) %>

<div class="admin-container">
  <!-- Sidebar Navigation -->
  <aside class="admin-sidebar">
    <div class="sidebar-header">
      <h2 class="logo">Admin<span>Portal</span></h2>
      <button class="sidebar-toggle d-md-none" id="mobileSidebarToggle">
        <i class="bi bi-x-lg"></i>
      </button>
    </div>
    
    <nav class="sidebar-nav">
      <ul>
        <li class="nav-item active">
          <a href="/admin/dashboard" class="nav-link">
            <i class="bi bi-speedometer2"></i>
            <span>Dashboard</span>
          </a>
        </li>
        
        <li class="nav-section">Management</li>
        
        <li class="nav-item">
          <a href="/admin/users" class="nav-link">
            <i class="bi bi-people"></i>
            <span>👥Users</span>
          </a>
        </li>
        
        <li class="nav-item">
          <a href="/admin/students" class="nav-link">
            <i class="bi bi-mortarboard"></i>
            <span>👩‍🎓Students</span>
          </a>
        </li>
        
        <li class="nav-item">
          <a href="/admin/workers" class="nav-link">
            <i class="bi bi-briefcase"></i>
            <span>👷‍♂️Workers</span>
          </a>
        </li>
        
        <li class="nav-item">
          <a href="/admin/courses" class="nav-link">
            <i class="bi bi-journal-bookmark"></i>
            <span>📚Courses</span>
          </a>
        </li>
        
        <li class="nav-section">Content</li>
        
        <li class="nav-item">
          <a href="/admin/testimonials" class="nav-link">
            <i class="bi bi-chat-square-quote"></i>
            <span>👩‍🍼Testimonials</span>
          </a>
        </li>
        
        <li class="nav-item">
          <a href="/admin/portfolios" class="nav-link">
            <i class="bi bi-collection"></i>
            <span>🅿️Portfolio</span>
          </a>
        </li>
        
        <li class="nav-section">Financial</li>
        
        <li class="nav-item">
          <a href="/admin/payments" class="nav-link">
            <i class="bi bi-credit-card"></i>
            <span>💲Payments</span>
          </a>
        </li>
      </ul>
    </nav>
    
    <div class="sidebar-footer">
      <div class="user-profile">
        <div class="avatar">
          <img src="/images/default-avatar.jpg" alt="Admin User">
        </div>
        <div class="user-info">
          <span class="name">Admin User</span>
          <span class="role">Super Admin</span>
        </div>
      </div>
      <a href="/logout" class="logout-btn">
        <i class="bi bi-box-arrow-right"></i>
      </a>
    </div>
  </aside>

  <!-- Main Content Area -->
  <main class="admin-main">
    <!-- Top Navigation Bar -->
    <header class="admin-header">
      <div class="header-left">
        <button class="sidebar-toggle d-none d-md-block" id="desktopSidebarToggle">
          <i class="bi bi-list"></i>
        </button>
        <nav aria-label="breadcrumb">
          <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="/">Home</a></li>
           
          </ol>
        </nav>
      </div>
      
      <div class="header-right">
        <div class="header-actions">
          <button class="btn btn-icon" id="notificationsBtn">
            <i class="bi bi-bell"></i>
            <span class="badge">3</span>
          </button>
          <button class="btn btn-icon" id="darkModeToggle">
            <i class="bi bi-moon"></i>
          </button>
        </div>
      </div>
    </header>

    <!-- Dashboard Content -->
    <div class="admin-content">
      <div class="content-header">
        <h1>Dashboard Overview</h1>
        <p class="subtitle">Welcome back! Here's what's happening with your platform today.</p>
        
        <div class="header-actions">
          <button class="btn btn-primary">
            <i class="bi bi-download"></i>
            <span>Export Report</span>
          </button>
        </div>
      </div>

      <!-- Stats Cards -->
      <div class="stats-grid">
        <div class="stat-card">
          <div class="stat-icon bg-primary">
            <i class="bi bi-people"></i>
          </div>
          <div class="stat-info" style="border-color:#ff7800; border-width: 10px; border-radius: 10px;">
            <h3><%= total_users %></h3>
            <p>Total Users</p>
            <span class="stat-trend positive">
              <i class="bi bi-arrow-up"></i> 5% from last week
            </span>
          </div>
        </div>
        
        <div class="stat-card">
          <div class="stat-icon bg-accent">
            <i class="bi bi-mortarboard"></i>
          </div>
          <div class="stat-info">
            <h3><%= total_students %></h3>
            <p>Total Students</p>
            <span class="stat-trend positive">
              <i class="bi bi-arrow-up"></i> 12% from last week
            </span>
          </div>
        </div>
        
        <div class="stat-card">
          <div class="stat-icon bg-secondary">
            <i class="bi bi-briefcase"></i>
          </div>
          <div class="stat-info">
            <h3><%= total_workers %></h3>
            <p>Total Workers</p>
            <span class="stat-trend positive">
              <i class="bi bi-arrow-up"></i> 3% from last week
            </span>
          </div>
        </div>
        
        <div class="stat-card">
          <div class="stat-icon bg-dark">
            <i class="bi bi-journal-bookmark"></i>
          </div>
          <div class="stat-info">
            <h3><%= total_courses %></h3>
            <p>Total Courses</p>
            <span class="stat-trend positive">
              <i class="bi bi-arrow-up"></i> 8% from last week
            </span>
          </div>
        </div>
      </div>

      <!-- Main Content Grid -->
      <div class="content-grid">
        <!-- Recent Activity Section -->
        <section class="content-section">
          <div class="section-header">
            <h2>Recent Activity</h2>
            <a href="#" class="btn btn-link">View All</a>
          </div>
          
          <div class="activity-list">
            <div class="activity-item">
              <div class="activity-icon bg-primary">
                <i class="bi bi-person-plus"></i>
              </div>
              <div class="activity-content">
                <h4>New user registration</h4>
                <p>John Doe registered 2 minutes ago</p>
              </div>
              <div class="activity-meta">
                <span class="badge badge-primary">New</span>
                <span class="time">2 min ago</span>
              </div>
            </div>
            
            <div class="activity-item">
              <div class="activity-icon bg-accent">
                <i class="bi bi-mortarboard"></i>
              </div>
              <div class="activity-content">
                <h4>New course enrollment</h4>
                <p>Jane Smith enrolled in Web Development</p>
              </div>
              <div class="activity-meta">
                <span class="time">15 min ago</span>
              </div>
            </div>
            
            <div class="activity-item">
              <div class="activity-icon bg-secondary">
                <i class="bi bi-cash-coin"></i>
              </div>
              <div class="activity-content">
                <h4>New payment received</h4>
                <p>$250 payment for Course XYZ</p>
              </div>
              <div class="activity-meta">
                <span class="badge badge-success">Paid</span>
                <span class="time">1 hour ago</span>
              </div>
            </div>
          </div>
        </section>

        <!-- Quick Actions Section -->
        <section class="content-section">
          <div class="section-header">
            <h2>Quick Actions</h2>
          </div>
          
          <div class="quick-actions">
            <button class="action-card" data-bs-toggle="modal" data-bs-target="#addUserModal">
              <div class="action-icon bg-primary-light">
                <i class="bi bi-person-plus"></i>
              </div>
              <h3>Add New User</h3>
              <p>Register new platform user</p>
            </button>
            
            <button class="action-card" data-bs-toggle="modal" data-bs-target="#createCourseModal">
              <div class="action-icon bg-accent-light">
                <i class="bi bi-journal-plus"></i>
              </div>
              <h3>Create Course</h3>
              <p>Add new learning course</p>
            </button>
            
            <button class="action-card" data-bs-toggle="modal" data-bs-target="#settingsModal">
              <div class="action-icon bg-secondary-light">
                <i class="bi bi-gear"></i>
              </div>
              <h3>System Settings</h3>
              <p>Configure platform settings</p>
            </button>
          </div>
        </section>
      </div>
    </div>
  </main>
</div>

<!-- Modals -->


<% include('../partials/footer') %>

<style>
  /* ===== Brand Colors ===== */
  :root {
    /* Your brand colors */
    --brand-primary: #010318;       /* Deep navy blue */
    --brand-light: #ffffff;         /* White */
    --brand-accent: #ff7800;        /* Vibrant orange */
    --brand-secondary: #1a1f3d;     /* Slightly lighter navy */
    --brand-dark: #010318;          /* Dark color for text */
    
    /* Derived colors */
    --primary-light: rgba(1, 3, 24, 0.1);
    --secondary-light: rgba(26, 31, 61, 0.1);
    --accent-light: rgba(255, 120, 0, 0.1);
    --success-color: #28a745;
    --warning-color: #ffc107;
    --danger-color: #dc3545;
    --info-color: #17a2b8;
    
    /* UI Colors */
    --border-color: #e9ecef;
    --sidebar-width: 280px;
    --header-height: 70px;
    --transition: all 0.3s ease;
    --shadow-sm: 0 1px 3px rgba(0,0,0,0.12);
    --shadow-md: 0 4px 6px rgba(0,0,0,0.1);
    --shadow-lg: 0 10px 15px rgba(0,0,0,0.1);
    --border-radius: 8px;
    --border-radius-sm: 4px;
  }

  /* Dark mode colors */
  [data-theme="dark"] {
    --brand-primary: #010318;
    --brand-secondary: #1a1f3d;
    --brand-light: #010318;
    --brand-dark: #ffffff;
    --border-color: #1a1f3d;
    --primary-light: rgba(255, 255, 255, 0.1);
  }

  /* ===== Base Styles ===== */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    background-color: var(--brand-light);
    color: var(--brand-dark);
    transition: var(--transition);
    line-height: 1.6;
  }

  /* ===== Layout Structure ===== */
  .admin-container {
    display: flex;
    min-height: 100vh;
  }

  .admin-sidebar {
    width: var(--sidebar-width);
    background-color: var(--brand-primary);
    height: 100vh;
    position: fixed;
    z-index: 100;
    transition: var(--transition);
    overflow-y: auto;
    box-shadow: var(--shadow-sm);
    color: white;
  }

  .admin-main {
    flex: 1;
    margin-left: var(--sidebar-width);
    transition: var(--transition);
  }

  .admin-content {
    padding: 2rem;
  }

  .admin-header {
    height: var(--header-height);
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 2rem;
    background-color: var(--brand-light);
    border-bottom: 1px solid var(--border-color);
    position: sticky;
    top: 0;
    z-index: 90;
  }

  /* ===== Sidebar Styles ===== */
  .sidebar-header {
    padding: 1.5rem 1.5rem 1rem;
    display: flex;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .logo {
    font-size: 1.5rem;
    font-weight: 700;
    color: white;
  }

  .logo span {
    color: var(--brand-accent);
  }

  .sidebar-toggle {
    background: none;
    border: none;
    font-size: 1.25rem;
    color: rgba(255, 255, 255, 0.7);
    cursor: pointer;
    transition: var(--transition);
  }

  .sidebar-toggle:hover {
    color: white;
  }

  .sidebar-nav {
    padding: 1rem 0;
  }

  .nav-section {
    padding: 0.75rem 1.5rem;
    font-size: 25px;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: rgba(255, 255, 255, 0.6);
    font-weight: 600;
    margin-top: 1rem;
    font-family: Arial, Helvetica, sans-serif;
  }

  .nav-item {
    margin: 0.25rem 0;
  }

  .nav-item{
    font-size: 20px;
    font-family: Georgia, 'Times New Roman', Times, serif;
   
  }

  .nav-link {
    display: flex;
    align-items: center;
    padding: 0.75rem 1.5rem;
    color: rgba(255, 255, 255, 0.8);
    text-decoration: none;
    border-radius: var(--border-radius);
    transition: var(--transition);
  }

  .nav-link i {
    font-size: 1.1rem;
    margin-right: 0.75rem;
    width: 24px;
    text-align: center;
   
  }

  .nav-link:hover {
    background-color: rgba(255, 255, 255, 0.1);
    color: white;
  }

  .nav-link.active {
    background-color: rgba(255, 255, 255, 0.1);
    color: white;
    font-weight: 500;
    border-left: 3px solid var(--brand-accent);
  }

  .sidebar-footer {
    padding: 1.5rem;
    margin-top: auto;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .user-profile {
    display: flex;
    align-items: center;
  }

  .avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    overflow: hidden;
    margin-right: 0.75rem;
  }

  .avatar img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .user-info {
    display: flex;
    flex-direction: column;
  }

  .user-info .name {
    font-size: 0.875rem;
    font-weight: 500;
    color: white;
  }

  .user-info .role {
    font-size: 0.75rem;
    color: rgba(255, 255, 255, 0.6);
  }

  .logout-btn {
    color: rgba(255, 255, 255, 0.7);
    font-size: 1.1rem;
    transition: var(--transition);
  }

  .logout-btn:hover {
    color: white;
  }

  /* ===== Header Styles ===== */
  .header-left {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .breadcrumb {
    display: flex;
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .breadcrumb-item {
    font-size: 0.875rem;
  }

  .breadcrumb-item a {
    color: var(--brand-secondary);
    text-decoration: none;
    transition: var(--transition);
  }

  .breadcrumb-item a:hover {
    color: var(--brand-accent);
  }

  .breadcrumb-item.active {
    color: var(--brand-dark);
    font-weight: 500;
  }

  .breadcrumb-item + .breadcrumb-item::before {
    content: "/";
    padding: 0 0.5rem;
    color: var(--brand-secondary);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 0.75rem;
  }

  .btn-icon {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    background: none;
    border: none;
    color: var(--brand-secondary);
    position: relative;
    transition: var(--transition);
  }

  .btn-icon:hover {
    background-color: var(--primary-light);
    color: var(--brand-accent);
  }

  .badge {
    position: absolute;
    top: -2px;
    right: -2px;
    background-color: var(--brand-accent);
    color: white;
    border-radius: 50%;
    width: 18px;
    height: 18px;
    font-size: 0.6rem;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* ===== Content Styles ===== */
  .content-header {
    margin-bottom: 2rem;
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .content-header h1 {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--brand-dark);
  }

  .subtitle {
    font-size: 0.875rem;
    color: var(--brand-secondary);
  }

  .header-actions {
    margin-top: 1rem;
  }

  .btn {
    padding: 0.5rem 1rem;
    border-radius: var(--border-radius);
    font-size: 0.875rem;
    font-weight: 500;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    cursor: pointer;
    transition: var(--transition);
    border: 1px solid transparent;
  }

  .btn-primary {
    background-color: var(--brand-accent);
    color: white;
    border-color: var(--brand-accent);
  }

  .btn-primary:hover {
    background-color: #e06b00;
    border-color: #e06b00;
    box-shadow: var(--shadow-sm);
  }

  .btn-secondary {
    background-color: white;
    color: var(--brand-secondary);
    border-color: var(--border-color);
  }

  .btn-secondary:hover {
    background-color: var(--brand-light);
    box-shadow: var(--shadow-sm);
  }

  .btn-link {
    background: none;
    border: none;
    color: var(--brand-accent);
    padding: 0;
    font-size: 0.875rem;
    font-weight: 500;
  }

  .btn-link:hover {
    text-decoration: underline;
  }

  /* ===== Stats Cards ===== */
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1.5rem;
    margin-bottom: 2rem;
  }

  .stat-card {
    background-color: var(--brand-light);
    border-radius: var(--border-radius);
    padding: 1.5rem;
    box-shadow: var(--shadow-sm);
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 1rem;
    border: 1px solid var(--border-color);
  }

  .stat-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
    border-color: var(--brand-accent);
  }

  .stat-icon {
    width: 48px;
    height: 48px;
    border-radius: var(--border-radius);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.5rem;
    color: white;
  }

  .stat-icon.bg-primary {
    background-color: var(--brand-primary);
  }

  .stat-icon.bg-accent {
    background-color: var(--brand-accent);
  }

  .stat-icon.bg-secondary {
    background-color: var(--brand-secondary);
  }

  .stat-icon.bg-dark {
    background-color: var(--brand-dark);
  }

 .stat-card{
  border: double  #010318;
  border-width: 15px;
  box-shadow: 0 8px 16px 2px #ff7800;

  
 }


  .stat-info h3 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 0.25rem;
  }

  .stat-info p {
    font-size: 0.875rem;
    color: var(--brand-secondary);
    margin-bottom: 0.25rem;
  }

  .stat-trend {
    font-size: 0.75rem;
    display: flex;
    align-items: center;
    gap: 0.25rem;
  }

  .stat-trend.positive {
    color: var(--success-color);
  }

  .stat-trend.negative {
    color: var(--danger-color);
  }

  /* ===== Content Grid ===== */
  .content-grid {
    display: grid;
    grid-template-columns: 2fr 1fr;
    gap: 1.5rem;
  }

  @media (max-width: 992px) {
    .content-grid {
      grid-template-columns: 1fr;
    }
  }

  .content-section {
    background-color: var(--brand-light);
    border-radius: var(--border-radius);
    box-shadow: var(--shadow-sm);
    overflow: hidden;
    border: 1px solid var(--border-color);
  }

  .section-header {
    padding: 1rem 1.5rem;
    border-bottom: 1px solid var(--border-color);
    display: flex;
    align-items: center;
    justify-content: space-between;
  }

  .section-header h2 {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--brand-dark);
  }

  /* ===== Activity List ===== */
  .activity-list {
    padding: 0.5rem 0;
  }

  .activity-item {
    padding: 1rem 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    transition: var(--transition);
    border-bottom: 1px solid var(--border-color);
  }

  .activity-item:last-child {
    border-bottom: none;
  }

  .activity-item:hover {
    background-color: var(--primary-light);
  }

  .activity-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    color: white;
  }

  .activity-icon.bg-primary {
    background-color: var(--brand-primary);
  }

  .activity-icon.bg-accent {
    background-color: var(--brand-accent);
  }

  .activity-icon.bg-secondary {
    background-color: var(--brand-secondary);
  }

  .activity-content {
    flex: 1;
  }

  .activity-content h4 {
    font-size: 0.875rem;
    font-weight: 500;
    margin-bottom: 0.25rem;
    color: var(--brand-dark);
  }


  .activity-content p {
    font-size: 0.75rem;
    color: var(--brand-secondary);
  }

  .activity-meta {
    display: flex;
    flex-direction: column;
    align-items: flex-end;
    gap: 0.25rem;
  }

  .time {
    font-size: 0.75rem;
    color: var(--brand-secondary);
  }

  .badge {
    padding: 0.25rem 0.5rem;
    border-radius: var(--border-radius-sm);
    font-size: 0.75rem;
    font-weight: 500;
  }

  .badge-primary {
    background-color: var(--primary-light);
    color: var(--brand-primary);
  }

  .badge-success {
    background-color: rgba(40, 167, 69, 0.1);
    color: var(--success-color);
  }

  /* ===== Quick Actions ===== */
  .quick-actions {
    padding: 1rem;
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  .action-card {
    background: none;
    border: none;
    text-align: left;
    padding: 1.5rem;
    border-radius: var(--border-radius);
    transition: var(--transition);
    cursor: pointer;
    border: 1px solid var(--border-color);
  }

  .action-card:hover {
    background-color: var(--primary-light);
    transform: translateY(-3px);
    box-shadow: var(--shadow-sm);
    border-color: var(--brand-accent);
  }

  .action-icon {
    width: 48px;
    height: 48px;
    border-radius: var(--border-radius);
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.25rem;
    margin-bottom: 1rem;
  }

  .action-icon.bg-primary-light {
    background-color: var(--primary-light);
    color: var(--brand-primary);
  }

  .action-icon.bg-accent-light {
    background-color: var(--accent-light);
    color: var(--brand-accent);
  }

  .action-icon.bg-secondary-light {
    background-color: var(--secondary-light);
    color: var(--brand-secondary);
  }

  .action-card h3 {
    font-size: 1rem;
    font-weight: 600;
    margin-bottom: 0.25rem;
    color: var(--brand-dark);
  }

  .action-card p {
    font-size: 0.875rem;
    color: var(--brand-secondary);
  }

  /* ===== Modal Styles ===== */
  .modal-content {
    border: none;
    border-radius: var(--border-radius);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
    background-color: var(--brand-light);
  }

  .modal-header {
    padding: 1.25rem 1.5rem;
    border-bottom: 1px solid var(--border-color);
  }

  .modal-title {
    font-size: 1.25rem;
    font-weight: 600;
    color: var(--brand-dark);
  }

  .modal-body {
    padding: 1.5rem;
  }

  .modal-footer {
    padding: 1rem 1.5rem;
    border-top: 1px solid var(--border-color);
    display: flex;
    justify-content: flex-end;
    gap: 0.75rem;
  }

  .modal-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-group label {
    font-size: 0.875rem;
    font-weight: 500;
    color: var(--brand-dark);
  }

  .form-group input,
  .form-group select,
  .form-group textarea {
    padding: 0.75rem;
    border: 1px solid var(--border-color);
    border-radius: var(--border-radius-sm);
    font-size: 0.875rem;
    transition: var(--transition);
    background-color: var(--brand-light);
    color: var(--brand-dark);
  }

  .form-group input:focus,
  .form-group select:focus,
  .form-group textarea:focus {
    outline: none;
    border-color: var(--brand-accent);
    box-shadow: 0 0 0 2px rgba(255, 120, 0, 0.2);
  }

  /* ===== Dark Mode Overrides ===== */
  [data-theme="dark"] {
    .admin-sidebar {
      background-color: var(--brand-primary);
    }
    
    .admin-header, 
    .admin-content,
    .content-section,
    .stat-card,
    .modal-content {
      background-color: var(--brand-primary);
      color: white;
      border-color: var(--brand-secondary);
    }
    
    .breadcrumb-item.active,
    .content-header h1,
    .section-header h2,
    .activity-content h4,
    .action-card h3,
    .modal-title,
    .form-group label {
      color: white;
    }
    
    .subtitle,
    .breadcrumb-item a,
    .stat-info p,
    .activity-content p,
    .action-card p,
    .time {
      color: rgba(255, 255, 255, 0.7);
    }
    
    .form-group input,
    .form-group select,
    .form-group textarea {
      background-color: var(--brand-secondary);
      color: white;
      border-color: var(--brand-secondary);
    }
    
    .btn-secondary {
      background-color: var(--brand-secondary);
      color: white;
      border-color: var(--brand-secondary);
    }
  }

  /* ===== Responsive Styles ===== */
  @media (max-width: 992px) {
    .admin-sidebar {
      transform: translateX(-100%);
    }
    
    .admin-sidebar.show {
      transform: translateX(0);
    }
    
    .admin-main {
      margin-left: 0;
    }
  }

  @media (min-width: 992px) {
    .sidebar-collapsed .admin-sidebar {
      width: 80px;
    }
    
    .sidebar-collapsed .admin-sidebar .logo span,
    .sidebar-collapsed .admin-sidebar .nav-link span,
    .sidebar-collapsed .admin-sidebar .user-info,
    .sidebar-collapsed .admin-sidebar .nav-section {
      display: none;
    }
    
    .sidebar-collapsed .admin-sidebar .nav-link {
      justify-content: center;
      padding: 0.75rem;
    }
    
    .sidebar-collapsed .admin-sidebar .nav-link i {
      margin-right: 0;
    }
    
    .sidebar-collapsed .admin-main {
      margin-left: 80px;
    }
  }
</style>

<script>
  // Dark Mode Toggle
  const darkModeToggle = document.getElementById('darkModeToggle');
  const html = document.documentElement;
  
  // Check for saved user preference
  const currentTheme = localStorage.getItem('theme') || 'light';
  html.setAttribute('data-theme', currentTheme);
  
  darkModeToggle.addEventListener('click', () => {
    const newTheme = html.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
    html.setAttribute('data-theme', newTheme);
    localStorage.setItem('theme', newTheme);
    
    // Update icon
    const icon = darkModeToggle.querySelector('i');
    icon.className = newTheme === 'dark' ? 'bi bi-sun' : 'bi bi-moon';
  });

  // Set initial icon
  const icon = darkModeToggle.querySelector('i');
  icon.className = currentTheme === 'dark' ? 'bi bi-sun' : 'bi bi-moon';

  // Sidebar Toggle
  const desktopSidebarToggle = document.getElementById('desktopSidebarToggle');
  const mobileSidebarToggle = document.getElementById('mobileSidebarToggle');
  const sidebar = document.querySelector('.admin-sidebar');
  
  desktopSidebarToggle?.addEventListener('click', () => {
    document.body.classList.toggle('sidebar-collapsed');
    localStorage.setItem('sidebarCollapsed', document.body.classList.contains('sidebar-collapsed'));
  });
  
  mobileSidebarToggle?.addEventListener('click', () => {
    sidebar.classList.toggle('show');
  });

  // Check for saved sidebar state
  if (localStorage.getItem('sidebarCollapsed') === 'true') {
    document.body.classList.add('sidebar-collapsed');
  }

  // Close sidebar when clicking outside on mobile
  document.addEventListener('click', (e) => {
    if (window.innerWidth < 992 && !sidebar.contains(e.target) && !mobileSidebarToggle.contains(e.target)) {
      sidebar.classList.remove('show');
    }
  });

  // Modal form submission (placeholder)
  document.querySelectorAll('.modal .btn-primary').forEach(btn => {
    btn.addEventListener('click', () => {
      // Add backend submission logic here
      console.log('Form submitted');
      bootstrap.Modal.getInstance(btn.closest('.modal')).hide();
    });
  });
</script>